name: Publish

# Prerelease channels:
#   staging branch → @beta  (integration, post-review features)
#   alpha branch   → @alpha (experimental, cutting-edge)
# @latest releases are fully automated by release.yml on merges to main.

on:
  push:
    branches: [staging, alpha]
  workflow_dispatch:

jobs:
  staging:
    name: Publish @beta
    if: github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    # id-token: write enables npm --provenance (signed attestation linking the
    # tarball to this workflow run) and is also a prerequisite if the @humanbased
    # scope is ever migrated to npm Trusted Publishers (OIDC-only, no NPM_TOKEN).
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history for tag detection and commit counting

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Compute next stable version (dry run)
        id: next_ver
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          dry_run: true
          default_bump: patch
          major_string_token: 'BREAKING CHANGE'
          minor_string_token: 'feat'
          patch_string_token: 'fix,perf,refactor'
          release_branches: staging   # include staging so dry-run computes new_version here

      - name: Stamp prerelease version
        id: stamp
        run: |
          # Use the computed next stable version so beta always sorts above the current @latest.
          # Fall back to a patch-bump of the current package.json version if the action returned nothing.
          NEXT="${{ steps.next_ver.outputs.new_version }}"
          if [ -z "$NEXT" ]; then
            CURRENT=$(node -p "require('./package.json').version.replace(/-.*$/, '')")
            IFS='.' read -r MAJ MIN PAT <<< "$CURRENT"
            NEXT="${MAJ}.${MIN}.$((PAT + 1))"
          fi
          # Count commits since the last vX.Y.Z tag; this is the monotonically increasing beta counter.
          LAST_TAG=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-version:refname | head -1)
          if [ -n "$LAST_TAG" ]; then
            COUNT=$(git rev-list --count "${LAST_TAG}..HEAD")
          else
            COUNT=$(git rev-list --count HEAD)
          fi
          VERSION="${NEXT}-beta.${COUNT}"
          npm version "$VERSION" --no-git-tag-version
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Publish to npm @beta
        run: |
          VERSION="${{ steps.stamp.outputs.version }}"
          # Idempotency: if a previous run already pushed this exact version (e.g. retry
          # after a transient failure), skip the publish so we don't trip npm's
          # "cannot publish over existing version" error and surface a real failure as
          # if it were that one.
          if npm view "@humanbased/crosscheck@${VERSION}" version 2>/dev/null | grep -qF "${VERSION}"; then
            echo "Version ${VERSION} already on npm — skipping publish."
            exit 0
          fi
          npm publish --tag beta --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  alpha:
    name: Publish @alpha
    if: github.ref == 'refs/heads/alpha'
    runs-on: ubuntu-latest
    environment: alpha
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history for tag detection and commit counting

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Compute next stable version (dry run)
        id: next_ver
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          dry_run: true
          default_bump: patch
          major_string_token: 'BREAKING CHANGE'
          minor_string_token: 'feat'
          patch_string_token: 'fix,perf,refactor'
          release_branches: alpha   # include alpha so dry-run computes new_version here

      - name: Stamp prerelease version
        id: stamp
        run: |
          NEXT="${{ steps.next_ver.outputs.new_version }}"
          if [ -z "$NEXT" ]; then
            CURRENT=$(node -p "require('./package.json').version.replace(/-.*$/, '')")
            IFS='.' read -r MAJ MIN PAT <<< "$CURRENT"
            NEXT="${MAJ}.${MIN}.$((PAT + 1))"
          fi
          LAST_TAG=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-version:refname | head -1)
          if [ -n "$LAST_TAG" ]; then
            COUNT=$(git rev-list --count "${LAST_TAG}..HEAD")
          else
            COUNT=$(git rev-list --count HEAD)
          fi
          VERSION="${NEXT}-alpha.${COUNT}"
          npm version "$VERSION" --no-git-tag-version
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Publish to npm @alpha
        run: |
          VERSION="${{ steps.stamp.outputs.version }}"
          if npm view "@humanbased/crosscheck@${VERSION}" version 2>/dev/null | grep -qF "${VERSION}"; then
            echo "Version ${VERSION} already on npm — skipping publish."
            exit 0
          fi
          npm publish --tag alpha --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
